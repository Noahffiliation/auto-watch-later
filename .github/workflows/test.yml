name: Run Tests

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
        cache: 'pip'

    - name: Install dependencies
      run: |
        if [ -f requirements.txt ]; then pip install --only-binary :all: --require-hashes -r requirements.txt; fi

    - name: Run tests with coverage
      run: |
        pytest --cov=auto_watch_later --cov-report=term-missing --cov-report=xml tests/

    - name: Verify coverage threshold
      run: |
        # Fail if coverage is less than 80%
        pytest --cov=auto_watch_later --cov-fail-under=80 tests/
